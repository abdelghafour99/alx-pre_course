#include <time.h>
#include <stdio.h>
/**
 * main - Prints numbers
 *        followed by a new line
 *
 * Return: Always 0
*/

int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
		printf("%d", a);

	printf('\n');

	return (0);
}
