#include "main.h"
#include <stdio.h>

/**
 * _strcat - concatenates two strings
 *
 *@dest: the first string
 *@src: the second one
 * Return: Always ptr
 */

char *_strcat(char *dest, char *src)
{
	char *ptr;

	ptr = strcat(dst, src);

	return (ptr);
}
