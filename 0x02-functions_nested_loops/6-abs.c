#include "main.h"

/**
 * _abs - the absolute value of an integer
 *
 * @n: the one be checked
 * Return: the absolute value of an integer
 */
int _abs(int n)
{

	if (n > 0)
	{
		return (n);
	}
	else
	{
		return ((0 - 1) * n);
	}

}
